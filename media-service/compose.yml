services:
  db:
    image: mongo
    container_name: piixl-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
    ports:
      - "8094:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: